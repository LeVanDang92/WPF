<Page x:Class="WpfApp1.FilterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FilterPage">

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="From Date:" FontWeight="SemiBold" FontSize="16"/>
            <DatePicker x:Name="FromDatePicker" Margin="0,0,0,8" Width="200"/>

            <TextBlock Text="To Date:" FontWeight="SemiBold" FontSize="16" Margin="20,0,0,0"/>
            <DatePicker x:Name="ToDatePicker" Margin="0,0,0,8" Width="200"/>
        </StackPanel>

        <DockPanel Grid.Row="1" Margin="0,8,0,8">
           <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Filter by:" FontWeight="SemiBold" FontSize="16"/>
                <ComboBox x:Name="FilterComboBox" Width="200" Margin="8,0,0,0">
                    <ComboBoxItem Content="-- Tất cả --"/>
                    <ComboBoxItem Content="Chờ xử lý"/>
                    <ComboBoxItem Content="Đang thực hiện"/>
                    <ComboBoxItem Content="Hoàn thành"/>
                    <ComboBoxItem Content="Đã hủy"/>
                </ComboBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <Button x:Name="ApplyFilterButton" Content="Apply Filter" Width="120" Height="32" Background="#007acc" Foreground="White" FontWeight="Bold"/>
                <Button x:Name="ClearFilterButton" Content="Clear Filter" Width="120" Height="32" Background="#e0e0e0" Margin="8,0,0,0"/>
            </StackPanel>
        </DockPanel>
        
        <DataGrid x:Name="FilterDataGrid" Grid.Row="2" AutoGenerateColumns="True" CanUserAddRows="False" Margin="0,8,0,0" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="Auto"/>
                <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
